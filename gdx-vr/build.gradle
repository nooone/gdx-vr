apply plugin: "java"

sourceCompatibility = 1.6

sourceSets {
	main {
		java {
			srcDirs = [ "src/" ]
		}

		resources {
			srcDirs = [ "src/" ]
		}
	}
}

eclipse.project {
    name = projectGroup + "-core"
}
